[phases.setup]
aptPkgs = ["libatomic1"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm run build",
  "apt-get clean",
  "rm -rf /var/lib/apt/lists/* /var/cache/apt/archives/* /tmp/*"
]

[start]
cmd = "npm run start"
